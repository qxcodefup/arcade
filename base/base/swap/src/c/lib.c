#include <stdio.h>

// Implemente a função 'troca'.
// ++
void troca(int *a, int *b){
    int aux = *a;
    *a = *b;
    *b = aux;
    // ==
    // Explicando o que tem que fazer
    // $$
    // (void) a;
    // (void) b;
}
// ==
void f2() {
    int x;
}

