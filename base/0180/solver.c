#include <stdio.h>
#include <stdlib.h>
int main(){
    for(int a = 0; a < 2; a++){
        int qtd = 0;
        char power[20];
        scanf("%d", &qtd);

        int values[qtd];
        char values[qtd][20];
        while(qtd--){
            scanf("%s", values[]);
            
        }
    }
    return 0;
}